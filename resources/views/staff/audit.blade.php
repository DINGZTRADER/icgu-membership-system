@extends('layouts.staff-admin')
@section('title','Audit Trail')
@section('page-title','Audit Trail')
@section('content')
<div class="hero"><div><span class="eyebrow">Read-only governance record</span><h2>Immutable operational audit trail</h2><p>Trace material application, membership, finance, renewal and portal-access actions without altering historical evidence.</p></div></div>
<div class="card section"><form class="filters" method="GET"><div class="field"><label>Action contains</label><input name="action" value="{{ $filters['action'] ?? '' }}" placeholder="payment, approval, renewal..."></div><div class="field"><label>User ID</label><input name="user_id" type="number" value="{{ $filters['user_id'] ?? '' }}"></div><button class="btn btn-primary" type="submit">Filter</button><a class="btn btn-soft" href="{{ route('staff.audit') }}">Reset</a></form></div>
<section class="section"><div class="table-wrap"><table class="table"><thead><tr><th>Timestamp</th><th>Actor</th><th>Action</th><th>Entity</th><th>Before</th><th>After</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at?->format('d M Y H:i:s') }}</td><td>{{ $log->user?->name ?? 'System' }}@if($log->user_id)<br><small>#{{ $log->user_id }}</small>@endif</td><td><span class="status info">{{ str_replace('_',' ',$log->action) }}</span></td><td class="mono">{{ class_basename($log->entity) }}{{ $log->entity_id ? ' #'.$log->entity_id : '' }}</td><td><div class="audit-payload">{{ $log->before_payload ? json_encode($log->before_payload,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) : '—' }}</div></td><td><div class="audit-payload">{{ $log->after_payload ? json_encode($log->after_payload,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) : '—' }}</div></td></tr>@empty<tr><td colspan="6" class="empty">No audit events match the filters.</td></tr>@endforelse</tbody></table></div><div class="pager"><span>Page {{ $logs->currentPage() }} of {{ $logs->lastPage() }}</span><div>@if($logs->previousPageUrl())<a href="{{ $logs->previousPageUrl() }}">Previous</a>@endif @if($logs->nextPageUrl())<a href="{{ $logs->nextPageUrl() }}">Next</a>@endif</div></div></section>
@endsection
